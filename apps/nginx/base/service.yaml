apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: web-prod
  labels:
    app.kubernetes.io/name: nginx
    app.kubernetes.io/part-of: web-platform
spec:
  selector:
    app.kubernetes.io/name: nginx
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
